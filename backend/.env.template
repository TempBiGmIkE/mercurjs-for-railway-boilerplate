STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:7000,http://localhost:9000,https://docs.medusajs.com
VENDOR_CORS=http://localhost:7000,https://docs.medusajs.com
# REDIS_URL=redis://localhost:6379 - only enable if having redis running locally
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=postgres://postgres:postgres@localhost/mercurjs
MEDUSA_ADMIN_EMAIL=admin@yourmail.com
MEDUSA_ADMIN_PASSWORD=supersecret

JWT_SECRET="1mu3wkkaeufohve87cb8vymepbil0dx0"
MEDUSA_ADMIN_EMAIL="admin@yourmail.com"
MEDUSA_ADMIN_PASSWORD="k1arvx30vdbqzj92hazwlu1tkklseeht"
MINIO_ACCESS_KEY="${{Bucket.MINIO_ROOT_USER}}"
MINIO_ENDPOINT="${{Bucket.MINIO_PUBLIC_HOST}}"
MINIO_SECRET_KEY="${{Bucket.MINIO_ROOT_PASSWORD}}"
NODE_ENV="production"
RAILWAY_HEALTHCHECK_TIMEOUT_SEC="720"
RAILWAY_PUBLIC_DOMAIN_VALUE="https://${{RAILWAY_PUBLIC_DOMAIN}}"
REDIS_URL="${{Redis.REDIS_PRIVATE_URL}}?family=0"
STORE_CORS="${{Marketplace.NEXT_PUBLIC_BASE_URL}}"
TEMPLATE_REPORTER_URL="https://railway-template-reporter-production.up.railway.app"
VENDOR_CORS="${{Vendor-dashboard.VITE_PUBLIC_BASE_URL}}"
# Algolia Configuration (Optional - enables automatic index creation and product syncing)
# ALGOLIA_APP_ID=your-application-id
# ALGOLIA_API_KEY=your-admin-api-key

# MinIO Storage Configuration (Optional - falls back to local storage)
# MINIO_ENDPOINT=your-minio-endpoint
# MINIO_ACCESS_KEY=your-access-key
# MINIO_SECRET_KEY=your-secret-key
# MINIO_BUCKET=custom-bucket-name # Optional - defaults to 'medusa-media'

# Stripe Payment Configuration (Optional - enables Stripe Connect payments)
# STRIPE_SECRET_API_KEY=sk_test_your_secret_key_here
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
